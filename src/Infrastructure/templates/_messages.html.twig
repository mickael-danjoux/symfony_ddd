{% if app.session.flashBag.peekAll is not empty %}
    {% for label, messages in app.flashes %}
            <div class="alert alert-{{ label }}">
                {% for message in messages %}
                    <p class="text-center mb-0">{{ message | raw }}</p>
                {% endfor %}
            </div>
    {% endfor %}
{% endif %}
